version: "3.9"

services:
  standalone-api:
    build:
      context: .
      dockerfile: deployment/standalone/Dockerfile
    env_file:
      - .env
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
